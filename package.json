{
  "name": "ohm-profile-vite",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "tailwindcss --input ./src/style.css --output ./src/dist.css --watch & vite",
    "build": "tailwindcss --input ./src/style.css --output ./src/dist.css --minify && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "tailwindcss": "^4.1.16",
    "vite": "^7.1.7"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.1.16"
  }
}
